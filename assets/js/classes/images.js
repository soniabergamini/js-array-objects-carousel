class Image {

    /** JSDoc Comment: explains Image class
     * 
     * @param {string} image / assign image path
     * @param {string} title / assign image title
     * @param {string} text  / assign image description
     */

    constructor(image, title, text) {
        this.image = image;
        this.title = title;
        this.text = text;
    }
}